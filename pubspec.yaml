name: rent_manager
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # Yeh versions aapke saare functions chalaenge aur bina error ke build honge
  file_picker: 6.1.1
  share_plus: 7.2.1
  shared_preferences: ^2.2.2
  flutter_plugin_android_lifecycle: ^2.0.17
  url_launcher: ^6.2.1
  image_picker: ^1.0.4
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

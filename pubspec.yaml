name: rent_manager
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # Stable aur fully compatible versions (android_lifecycle hata diya gaya hai)
  file_picker: 8.0.0
  share_plus: 7.2.1
  shared_preferences: 2.2.2
  url_launcher: 6.2.5
  image_picker: 1.0.7
  path_provider: 2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

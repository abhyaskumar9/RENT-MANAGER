name: rent_manager
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Aapke code me use hone wale saare required packages ab yahan add kar diye hain
  file_picker: ^8.1.7
  shared_preferences: ^2.3.5
  flutter_plugin_android_lifecycle: ^2.0.24
  url_launcher: ^6.3.1
  image_picker: ^1.1.2
  path_provider: ^2.1.3
  share_plus: ^10.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

name: rent_manager
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Icons aur standard fonts
  cupertino_icons: ^1.0.6

  # Updated versions jo ek dusre ke sath compatible hain
  file_picker: ^8.0.7
  share_plus: ^10.0.2
  shared_preferences: ^2.3.5
  flutter_plugin_android_lifecycle: ^2.0.24
  url_launcher: ^6.3.0
  image_picker: ^1.1.2
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
